using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class 梗圖事件 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    void 關閉(){
        this.gameObject.SetActive(false);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
